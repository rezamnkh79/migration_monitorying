# Database Configuration
MYSQL_HOST=46.245.77.98
MYSQL_USER=root
MYSQL_PASSWORD=mauFJcuf5dhRMQrjj
MYSQL_DATABASE=adtrace_db_stage
MYSQL_PORT=3306

# PostgreSQL Configuration - Using localhost since services use host network
POSTGRES_HOST=127.0.0.1
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=adtrace_live_20250805
POSTGRES_PORT=5432

# Redis Configuration - Connect to host Redis from Docker container
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Kafka Configuration (change to external IP if needed)
KAFKA_BOOTSTRAP_SERVERS=kafka:29092
KAFKA_UI_PORT=8080

# External Services Configuration (uncomment and modify if using external services)
# POSTGRES_HOST=127.0.0.1
# REDIS_HOST=127.0.0.1
# KAFKA_BOOTSTRAP_SERVERS=127.0.0.1:9092

# Monitoring Configuration
MONITORING_PORT=4000
VALIDATOR_PORT=9000
VALIDATOR_URL=http://172.17.0.1:9000

# Kafka Connect Configuration
KAFKA_CONNECT_PORT=8083
KAFKA_CONNECT_URL=http://connect:8083

# Logging
LOG_LEVEL=INFO

# Security (for production)
# JWT_SECRET=your-secret-key
# SSL_CERT_PATH=/path/to/cert
# SSL_KEY_PATH=/path/to/key
